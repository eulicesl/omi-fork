# Verification evidence

One folder per bug (`BUG-XXX/`). Drop runtime evidence here during on-macOS verification:
screenshots (`*.png`), recordings (`*.mov`), log excerpts (`*.log`/`*.txt`), TSan reports,
crash traces, and XCTest output. Reference these paths from the filled-in REPORT-TEMPLATE.md.

Empty until verification runs on a real Mac — this was scaffolded on Linux and nothing was executed.
